name: Migration Stage 3

on:
  workflow_dispatch:
    inputs:
      migration_data:
        description: 'Migration data in JSON format'
        required: true

jobs:
  world-1-3:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Stage 3
        run: |
          python scripts/stage_runner.py \
            --stage "World 1-3" \
            --data '${{ inputs.migration_data }}'